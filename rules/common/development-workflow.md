# Development Workflow

Standard workflow: implement per task brief, run build and test commands, verify acceptance criteria. For commit and PR conventions see [git-workflow.md](./git-workflow.md).

Orchestrator task briefs carry workflow instructions when needed.

---

This file is universal and is not extended by framework-specific rule files. For git conventions see `git-workflow.md`.
